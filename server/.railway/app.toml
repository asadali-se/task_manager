[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn taskmanager.wsgi:application --bind 0.0.0.0:$PORT --workers 2"
healthcheckPath = "/api/tasks/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
