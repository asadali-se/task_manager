{
  "name": "task-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build",
    "install": "cd client && npm install"
  },
  "dependencies": {
    "next": "16.2.4",
    "vercel": "^52.0.0"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
